describe('Parity App App', () => {
  it('loads the app properly without severe errors', () => {
    // Basic test just to ensure Cypress is configured correctly
    // The app would be tested with `pnpm dev` running
    expect(true).to.equal(true)
  })
})
